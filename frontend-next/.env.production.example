# Frontend production env vars.
# Set these in Vercel dashboard → Project → Settings → Environment Variables.
# DO NOT commit .env.production with real values.

# Public backend URL (Render). Used by next.config.mjs rewrites
# to proxy /api/* requests from the browser to the FastAPI backend.
NEXT_PUBLIC_API_URL=https://krishiai-api.onrender.com
